// ============================================================
// APP COLORS — GetWork App
// Design: Warm, Friendly, Modern, Crispy Emerald & Orange
// ============================================================

import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // ── Primary: Deep Crisp Emerald Green ───────────────────────
  static const Color primary = Color(0xFF00684A);
  static const Color primaryLight = Color(0xFF008B62);
  static const Color primaryDark = Color(0xFF004D36);
  static const Color primaryContainer = Color(0xFFD0F2E5);
  static const Color onPrimary = Color(0xFFFFFFFF);
  static const Color onPrimaryContainer = Color(0xFF002114);

  // ── Accent: Warm Orange ──────────────────────────────────────
  static const Color accent = Color(0xFFE85D04);
  static const Color accentLight = Color(0xFFFF8533);
  static const Color accentDark = Color(0xFFB34200);
  static const Color accentContainer = Color(0xFFFFDCC8);
  static const Color onAccent = Color(0xFFFFFFFF);

  // ── Background: Soft Warm Off-White ──────────────────────────
  static const Color background = Color(0xFFF6F6F2);
  static const Color surface = Color(0xFFFFFFFF);
  static const Color surfaceVariant = Color(0xFFEEEEE8);
  static const Color surfaceElevated = Color(0xFFF9F9F6);

  // ── Text ─────────────────────────────────────────────────────
  static const Color textPrimary = Color(0xFF181C1A);
  static const Color textSecondary = Color(0xFF6E7471);
  static const Color textHint = Color(0xFFA0A6A3);
  static const Color textOnDark = Color(0xFFFFFFFF);

  // ── Status Colors ─────────────────────────────────────────────
  static const Color success = Color(0xFF00684A);
  static const Color successLight = Color(0xFFE6F5F0);
  static const Color warning = Color(0xFFFF9800);
  static const Color warningLight = Color(0xFFFFF3E0);
  static const Color error = Color(0xFFE53935);
  static const Color errorLight = Color(0xFFFFEBEE);
  static const Color info = Color(0xFF2196F3);
  static const Color infoLight = Color(0xFFE3F2FD);

  // ── Job Category Colors ──────────────────────────────────────
  static const Color categoryDelivery = Color(0xFF00684A);
  static const Color categoryRetail = Color(0xFFE85D04);
  static const Color categoryFood = Color(0xFFFF6B6B);
  static const Color categoryConstruction = Color(0xFF8D6E63);
  static const Color categoryCleaning = Color(0xFF4FC3F7);
  static const Color categoryTech = Color(0xFF7C4DFF);
  static const Color categoryEvents = Color(0xFFFFD54F);

  // ── Map Marker Colors ────────────────────────────────────────
  static const Color markerDefault = Color(0xFF00684A);
  static const Color markerUrgent = Color(0xFFE85D04);
  static const Color markerApplied = Color(0xFF2196F3);

  // ── Borders & Dividers ───────────────────────────────────────
  static const Color border = Color(0xFFE2E4E1);
  static const Color divider = Color(0xFFEDEDE9);

  // ── Shadows ──────────────────────────────────────────────────
  static const Color shadowLight = Color(0x0D000000);
  static const Color shadowMedium = Color(0x1F000000);
}
