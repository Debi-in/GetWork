// ============================================================
// APP COLORS — GetWork App
// Design: Warm, Friendly, Modern | Material Design 3
// ============================================================
// Primary:    Sage Green   → trustworthy, nature, growth
// Accent:     Warm Orange  → energy, opportunity, action
// Background: Warm Ivory   → clean, soft, approachable
// ============================================================

import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // ── Primary: Sage Green ──────────────────────────────────────
  static const Color primary = Color(0xFF5B8A6F);
  static const Color primaryLight = Color(0xFF7AAF8E);
  static const Color primaryDark = Color(0xFF3D6B52);
  static const Color primaryContainer = Color(0xFFD4EDE0);
  static const Color onPrimary = Color(0xFFFFFFFF);
  static const Color onPrimaryContainer = Color(0xFF002114);

  // ── Accent: Warm Orange ──────────────────────────────────────
  static const Color accent = Color(0xFFE8773A);
  static const Color accentLight = Color(0xFFFF9A5C);
  static const Color accentDark = Color(0xFFBF5A22);
  static const Color accentContainer = Color(0xFFFFDCC8);
  static const Color onAccent = Color(0xFFFFFFFF);

  // ── Background: Warm Ivory ───────────────────────────────────
  static const Color background = Color(0xFFFAF8F5);
  static const Color surface = Color(0xFFFFFFFF);
  static const Color surfaceVariant = Color(0xFFF2EDE8);
  static const Color surfaceElevated = Color(0xFFF7F4F0);

  // ── Text ─────────────────────────────────────────────────────
  static const Color textPrimary = Color(0xFF1A1A2E);
  static const Color textSecondary = Color(0xFF6B6B80);
  static const Color textHint = Color(0xFFADADB8);
  static const Color textOnDark = Color(0xFFFFFFFF);

  // ── Status Colors ─────────────────────────────────────────────
  static const Color success = Color(0xFF4CAF50);
  static const Color successLight = Color(0xFFE8F5E9);
  static const Color warning = Color(0xFFFF9800);
  static const Color warningLight = Color(0xFFFFF3E0);
  static const Color error = Color(0xFFE53935);
  static const Color errorLight = Color(0xFFFFEBEE);
  static const Color info = Color(0xFF2196F3);
  static const Color infoLight = Color(0xFFE3F2FD);

  // ── Job Category Colors ──────────────────────────────────────
  static const Color categoryDelivery = Color(0xFF5B8A6F);
  static const Color categoryRetail = Color(0xFFE8773A);
  static const Color categoryFood = Color(0xFFFF6B6B);
  static const Color categoryConstruction = Color(0xFF8D6E63);
  static const Color categoryCleaning = Color(0xFF4FC3F7);
  static const Color categoryTech = Color(0xFF7C4DFF);
  static const Color categoryEvents = Color(0xFFFFD54F);

  // ── Map Marker Colors ────────────────────────────────────────
  static const Color markerDefault = Color(0xFF5B8A6F);
  static const Color markerUrgent = Color(0xFFE8773A);
  static const Color markerApplied = Color(0xFF2196F3);

  // ── Borders & Dividers ───────────────────────────────────────
  static const Color border = Color(0xFFE0DBD5);
  static const Color divider = Color(0xFFECE8E3);

  // ── Shadows ──────────────────────────────────────────────────
  static const Color shadowLight = Color(0x0F000000);
  static const Color shadowMedium = Color(0x1A000000);
}
